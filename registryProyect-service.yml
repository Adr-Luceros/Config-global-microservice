server:
  port: ${PORT:8761}
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/
#message: Servicio de registro y descubrimiento levantado en el servidor
